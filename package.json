{
  "name": "matchmaking-caldera",
  "version": "1.0.0",
  "description": "Matchmaking contract for Caldera deployment",
  "scripts": {
    "test": "hardhat test ",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-calculator.js --network shield",
    "node": "hardhat node",
    "test:local": "hardhat test --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.1"
  },
  "dependencies": {
    "ethers": "^6.13.4",
    "paillier-bigint": "^3.4.3"
  }
}
